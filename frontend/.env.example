# Local development
API_BASE_URL=http://127.0.0.1:8000
NEXT_PUBLIC_APP_ENV=development

# Production (Docker) — set in docker-compose.prod.yml:
# API_BASE_URL=http://api:8000
# NEXT_PUBLIC_APP_ENV=production
